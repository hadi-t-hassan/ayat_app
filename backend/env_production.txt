# Production Environment Configuration for ayat.pingtech.dev
# Copy this file to .env on your production server

# Django Settings
SECRET_KEY=your-production-secret-key-here-change-this
DEBUG=False
ALLOWED_HOSTS=ayat.pingtech.dev,www.ayat.pingtech.dev,localhost,127.0.0.1

# Database Configuration (MySQL)
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=ayat_events
DATABASE_USER=ayat_user
DATABASE_PASSWORD=your_secure_mysql_password
DATABASE_HOST=localhost
DATABASE_PORT=3306

# CORS Settings
CORS_ALLOWED_ORIGINS=https://ayat.pingtech.dev,https://www.ayat.pingtech.dev

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME_DAYS=1
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Static and Media Files
STATIC_URL=/static/
STATIC_ROOT=/home/ayat_app/backend/staticfiles
MEDIA_URL=/media/
MEDIA_ROOT=/home/ayat_app/backend/media

# Email Configuration (for future use)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Security Settings
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True
X_FRAME_OPTIONS=DENY
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# Session settings
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
CSRF_COOKIE_HTTPONLY=True

# Language Settings
LANGUAGE_CODE=en-us
TIME_ZONE=UTC
USE_I18N=True
USE_TZ=True

# API Settings
API_PAGE_SIZE=20
API_MAX_PAGE_SIZE=100

# Logging
LOG_LEVEL=INFO
LOG_FILE=/var/log/ayat_app/django.log
